using UnityEngine;

public class NPCBehaviour : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void RandomizeVote() 
    {
        // If the npc got to it's location

        // The crowd gone discuss what they want

        // Then the crowd displays what their decision is

        // A vote should be the same for all the NPC at the same location

        // So each location has it's own vote system
    }
}
